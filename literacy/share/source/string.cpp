#include <iostream> /* C++ 标准输入输出头文件 */

int main(int argc, char* argv[]) {
    /* 数组式初始化 */
    // 定义长度为10的字符数组，但初始化列表只设置了数组的前8个元素
    char chars[10] = { '1', '2', '3', '4', '5', '6', '7', '8' };
    // 定义字符串无需指明长度，初始化列表里有多少算多少，其无需操作结尾的'\0'
    std::string str = { 's', 't', 'r', 'i', 'n', 'g' };
    // END

    return 0;
}
